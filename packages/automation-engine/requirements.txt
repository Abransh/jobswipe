# JobSwipe Automation Engine Requirements

# Core dependencies (browser automation)
playwright>=1.40.0
pydantic[email]>=2.0.0
email-validator>=2.0.0

# LLM providers (browser-use compatibility)
anthropic>=0.18.0
openai>=1.0.0
google-generativeai>=0.3.0

# Async HTTP client (for API calls)
aiohttp>=3.9.0
httpx>=0.25.0

# Data validation and parsing
python-dotenv>=1.0.0

browser-use>=0.9.7
cdp-use>=1.4.4

# Logging and monitoring
structlog>=23.2.0

# Optional: Proxy rotation (server mode)
aiohttp-socks>=0.8.0

# Browser-use library (from parent directory)
# Note: This will be installed via path dependency
